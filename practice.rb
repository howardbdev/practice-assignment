# create a method, called print_array, that takes an argument of an array and uses #each to iterate through the array and puts each element.

a = [1, 2, 3, 4, 5]

def print_array(array)
  array.each { |a| puts block } 
  
  
end